// On GitHub: Pages must use “GitHub Actions” as the source so CI can overwrite this file
// from secret N8N_CONTACT_WEBHOOK_URL. Branch-only Pages never runs that step.
// For local testing, paste your n8n Production webhook URL below (do not commit secrets).
window.__N8N_CONTACT_WEBHOOK__ = "";
